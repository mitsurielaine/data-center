{
  "name": "data-center-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API REST para el Trabajo Práctico de Administración de Data Center (To-Do List con PostgreSQL)",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/routes/tasks.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
