# Copia este archivo como ".env" en la raíz del proyecto (NO se sube a Git).
# docker-compose lo lee automáticamente.
DB_USER=taskuser
DB_PASSWORD=cambia_esto_por_una_contrasena_fuerte
DB_NAME=tasksdb
